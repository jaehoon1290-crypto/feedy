import { Schema, model } from 'mongoose';
export const Order = model('Order', new Schema({ userId: { type: Schema.Types.ObjectId, ref: 'User', required: true }, storeId: { type: Schema.Types.ObjectId, ref: 'Store', required: true }, storeName: String, items: [{ menuId: Schema.Types.ObjectId, name: String, price: Number, quantity: Number }], deliveryAddress: { label: String, address: String, detail: String, latitude: Number, longitude: Number }, requests: { storeNote: String, deliveryNote: String, needsUtensils: { type: Boolean, default: false } }, paymentMethod: { type: String, enum: ['CARD_ON_DELIVERY', 'CASH_ON_DELIVERY'], required: true }, subtotal: Number, deliveryFee: Number, totalAmount: Number, status: { type: String, enum: ['PENDING','CONFIRMED','PREPARING','DELIVERY_REQUESTED','DELIVERING','COMPLETED','CANCELLED'], default: 'PENDING' } }, { timestamps: true }));
