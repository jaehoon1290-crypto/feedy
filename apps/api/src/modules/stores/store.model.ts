import { Schema, model } from 'mongoose';
const menuSchema = new Schema({ name: String, description: String, price: Number, imageUrl: String, options: [{ name: String, choices: [{ name: String, price: Number }] }], isSoldOut: { type: Boolean, default: false } }, { _id: true });
export const Category = model('Category', new Schema({ name: { type: String, required: true, unique: true }, imageUrl: String }, { timestamps: true }));
export const Store = model('Store', new Schema({ ownerId: { type: Schema.Types.ObjectId, ref: 'User' }, name: { type: String, unique: true }, categoryId: { type: Schema.Types.ObjectId, ref: 'Category' }, categoryIds: [{ type: Schema.Types.ObjectId, ref: 'Category' }], address: String, phone: String, description: String, imageUrl: String, deliveryFee: { type: Number, default: 0 }, minimumOrder: Number, favoriteCount: { type: Number, default: 0 }, estimatedDeliveryMinutes: Number, isOpen: { type: Boolean, default: true }, businessHours: [{ day: String, open: String, close: String, isClosed: Boolean }], menus: [menuSchema] }, { timestamps: true }));
